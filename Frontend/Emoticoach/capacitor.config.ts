import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.Emoticoach',
  appName: 'Emoticoach',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
